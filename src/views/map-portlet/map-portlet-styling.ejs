<style>
  .map-header {
    display: flex;
    flex-direction: column;
    width: 100%;
    margin: 0;
    padding: 0 50px 0;
    box-sizing: border-box;
  }
  .map-title {
    font-weight: 500;
    font-size: 30px;
    margin: 0;
    padding-left: 5px;
  }
  .map-header-bottom {
    width: 100%;
    height: 5px;
    margin-left: 50px;
    background: #263550;
  }
  .map-container {
    height: 450px;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    page-break-after: always;
  }

  .map-chartdiv {
    width: 700px;
    height: 450px;
  }

  .blob {
    border-radius: 50%;
    border: 2px solid rgba(255, 121, 63, 1);
    height: 16px;
    width: 16px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .inside-blob {
    background: rgba(255, 121, 63, 1);
    border-radius: 50%;
    height: 6px;
    width: 6px;
  }

  .map-table-container {
    margin: 30px 50px 0 50px;
  }

  .map-table-heading-container {
    font-size: 18px;
    break-inside: avoid;
    page-break-inside: avoid;
  }

  .map-table-heading {
    font-weight: bold;
  }

  .map-output-table {
    margin-top: 5px;
  }

  #pdf-other-tables {
    page-break-after: always;
  }
</style>
